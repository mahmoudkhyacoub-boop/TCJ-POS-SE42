# Release Notes V192 — Shop Identity and Flexible Permissions

## التعديلات المنفذة

أضيفت إعدادات المدير لتغيير اسم المحل بالعربية، اسم المحل بالإنجليزية، رقم الهاتف، الموقع، ومسار الشعار من واجهة إعدادات النظام. يتم حفظ الإعدادات في جدول `settings`، ويُستخدم الاسم والشعار ديناميكياً في واجهة البرنامج والفواتير وصور عقود استلام وتسليم أجهزة الصيانة.

تم توحيد خط عقود استلام وتسليم الأجهزة على `Cocon® Next Arabic Bold` مع بديل آمن عند عدم توفر الملف. كما أضيف الشعار إلى رأس العقد، وأضيف الاسم العربي والإنجليزي إلى الفواتير والعقود.

أضيف جدول `user_permissions` وحقل `users.permissions` لتخزين الصلاحيات الفردية. يستطيع المدير تحديد كل صلاحية أو إلغائها لكل مستخدم من زر `إدارة الصلاحيات الفردية`، دون الاعتماد على مسميات كاشير أو موظف مخزون أو موظف صيانة. المدير يحتفظ بصلاحية كاملة، والموظف يرى في قائمة التنقل فقط الواجهات المسموح بها.

## المحافظة على التوافق

تم الإبقاء على حقل `role` القديم للتوافق مع قواعد البيانات الحالية، ولم تُحذف أي بيانات أو قيود أو عمليات محاسبية. تتم إضافة الأعمدة والجداول الجديدة بطريقة آمنة عند تشغيل النسخة على قاعدة قديمة.

## التحقق

```text
PY_COMPILE=0
IDENTITY_PERMISSIONS_FULL_TEST=PASS
PASS=32
FAIL=0
```

تشمل الاختبارات التوافق، القيود المحاسبية، الحذف والتعديل، حفظ العملاء، الواجهة، الصور المضمنة، الهوية، الصلاحيات، وربط الخط والشعار بالمخرجات.
